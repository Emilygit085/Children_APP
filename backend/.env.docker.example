# 复制为 .env.docker 后填写真实值（勿提交 .env.docker）。
# 供 docker-compose.db.yml 中的 MySQL 容器使用。
#
# 应用连接串需与此处账号一致，例如 backend/.env：
# DATABASE_URL=mysql+pymysql://social_app:同下MYSQL_PASSWORD@127.0.0.1:3306/social_app

MYSQL_ROOT_PASSWORD=admin123
MYSQL_USER=social_app
MYSQL_PASSWORD=sha138138
